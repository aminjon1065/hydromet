[www]
; Expose the FPM status page on the loopback interface only. The container
; health check uses it; it is never routed through Nginx.
pm.status_path = /fpm-status

clear_env = no
catch_workers_output = yes
decorate_workers_output = no

access.log = /proc/self/fd/2
